fastapi==0.111.0
uvicorn[standard]==0.30.1
python-dotenv==1.0.1
supabase==2.6.0
# Tencent Cloud AI3D SDK and dependencies
# Using tencentcloud-sdk-python for generic services, plus the ai3d package path
# If ai3d submodule is not available via pip yet, the generic SDK will be used.
tencentcloud-sdk-python-ai3d==3.0.1424
# If the specific ai3d version is available, include it; otherwise the import path in code must be vendored.
# tencentcloud-ai3d==0.0.0  # Uncomment if package is published

# Optional: If image handling needed later
# pillow==10.4.0
